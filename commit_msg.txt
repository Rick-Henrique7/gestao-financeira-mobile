feat(notifications): trocar trofeu por engrenagem+sino e adicionar NotificationsModal

Mudancas no header do Hero (Home):
- Trofeu -> SINO (abre NotificationsModal)
- Sino -> ENGRENAGEM (navega para /settings)

NOVO COMPONENTE (src/components/NotificationsModal.tsx)
- Lista contas a pagar vencendo dentro do periodo configurado
  (settings.alert_days_before, default 3 dias)
- Mostra contas ATRASADAS (vermelho), que VENCEM HOJE (amarelo),
  e PROXIMAS (verde-limao)
- Cada item: icone + titulo + categoria + data + dias restantes + valor
- Respeita settings.notify_due_soon: se desativado, mostra mensagem
  "Alertas desativados"
- Se nenhuma conta no alerta: empty state com check verde

NOVO SETTING: alert_days_before (migration 006)
- Migration 006 adiciona coluna alert_days_before INTEGER DEFAULT 3
- UserSettings.alert_days_before: number
- settings.ts atualiza getSettings (fallback) e updateSettings (coluna
  incluida no UPDATE)

SETTINGS: novo controle "Periodo de alerta"
- 5 chips: 3d / 7d / 12d / 15d / 30d
- Click no chip salva alert_days_before
- Sub do toggle "Contas a vencer" agora reflete o valor atual
  (ex: "Avisar quando faltar ate 7 dias para o vencimento")
- Sub do "Periodo de alerta" avisa se o toggle esta desativado

MANUTENCAO
- Adicionado tipo 'chips' no SettingsEntry (right: 'chips' | chips[])
- Pressable da row desabilitado quando right === 'chips' (so os
  chips individuais sao interativos)
- Nova StyleSheet chipS (chip, chipActive, chipText, chipTextActive)

VERIFICACOES
- tsc --noEmit: 0 erros
- Versao 0.6.3 -> 0.7.0

FLUXO DO USUARIO
1. Tap no sino (header) -> abre NotificationsModal
2. Tap na engrenagem (header) -> vai pra /settings
3. Em Settings > Notificacoes > "Periodo de alerta" escolhe 12d (etc)
4. Volta pra Home, abre o sino -> ve contas vencendo em ate 12 dias
